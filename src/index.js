import React, {Component} from 'react';
import ReactDOM from 'react-dom/client';
// import App from './App';
import TaiwanMap from './test/taiwanMap';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TaiwanMap />
  </React.StrictMode>
);
